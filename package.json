{
  "name": "prediction-hub-proxy",
  "version": "1.0.0",
  "description": "Backend proxy that holds the football-data.org API key server-side and serves Prediction Hub's frontend, so the key never ships to the browser.",
  "type": "commonjs",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
